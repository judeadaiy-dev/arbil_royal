name: arbil_royal
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.5.0 <4.0.0' # هذا التعديل يضمن توافق أفضل مع إصدارات Flutter الحديثة

dependencies:
  flutter:
    sdk: flutter
  supabase_flutter: ^2.6.0
  google_fonts: ^6.2.1
  cached_network_image: ^3.4.0
  flutter_map: ^7.0.2
  latlong2: ^0.9.1
  connectivity_plus: ^6.0.5
  url_launcher: ^6.3.0
  flutter_localizations:
    sdk: flutter
  cupertino_icons: ^1.0.8

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
